<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!--
      Saving a share card to the gallery. API 29+ writes to MediaStore with no permission
      at all (scoped storage), so this is capped at 28 rather than requested app-wide.
      See ui/components/GallerySaver.kt.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".PaperBoxdApp"
        android:allowBackup="false"
        android:icon="@mipmap/app_icon"
        android:roundIcon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PaperBoxd">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.PaperBoxd">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Deep links: paperboxd.in book + profile pages only. Without the path
              prefixes this filter claims every paperboxd.in URL — including /privacy,
              /terms and the marketing pages, which the app cannot render.
              Handled in NavGraph via Screen.BookDetail.deepLink / Screen.Profile.deepLink.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="paperboxd.in" android:pathPrefix="/b/" />
                <data android:scheme="https" android:host="paperboxd.in" android:pathPrefix="/u/" />
            </intent-filter>
        </activity>

        <!-- Serves rendered share cards (cacheDir/shares) to the system share sheet. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
